# Configuración de notificaciones en tiempo real
# =============================================

# Broadcasting (Pusher/WebSockets)
BROADCAST_CONNECTION=pusher
# Para desarrollo local, puedes usar 'log' o 'null'
# BROADCAST_CONNECTION=log

# Configuración de Pusher (para producción)
PUSHER_APP_ID=your_app_id
PUSHER_APP_KEY=your_app_key
PUSHER_APP_SECRET=your_app_secret
PUSHER_APP_CLUSTER=mt1
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https

# Variables para Vite (Frontend)
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Configuración alternativa con Laravel Reverb (más moderna)
# REVERB_APP_ID=your_reverb_app_id
# REVERB_APP_KEY=your_reverb_key
# REVERB_APP_SECRET=your_reverb_secret
# REVERB_HOST=localhost
# REVERB_PORT=8080
# REVERB_SCHEME=http

# Queue (para procesar eventos en segundo plano)
QUEUE_CONNECTION=database
# En producción se recomienda usar redis o otro driver

# Configuración adicional de notificaciones
NOTIFICATION_SOUND_ENABLED=true
NOTIFICATION_AUTO_REFRESH=true
NOTIFICATION_REFRESH_INTERVAL=30000

# Configuración de canales
NOTIFICATION_CHANNELS=database,broadcast